
=== TAMAÑO DEL ARCHIVO ===

Name     Length
----     ------
page.tsx   3743



=== PRIMERAS 30 LINEAS ===
'use client';

import { useEffect, useState } from 'react';
import Link from 'next/link';
import { supabase } from '@/lib/supabase';

export default function PanelJuezPage() {
  const [competencias, setCompetencias] = useState<any[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const fetchUserAndCompetencias = async () => {
      const {
        data: { user },
      } = await supabase.auth.getUser();

      if (user) {

        // Obtener jueces del usuario
        const { data: jueces } = await supabase
          .from('jueces')
          .select('id, concurso_id, letra_oficial')
          .eq('user_id', user.id);

        if (jueces && jueces.length > 0) {
          // Obtener todas las competencias para estos jueces
          const jueceIds = jueces.map((j) => j.id);
          const { data: competenciasData } = await supabase
            .from('competencias')
            .select(

=== BUSCAR PALABRAS CLAVE ===
--- Buscar 'prueba_jueces':
--- Buscar 'Panel del Juez':

      <h1 className="text-3xl font-bold mb-6">â­ Panel del Juez</h1>


--- Buscar 'jornada' (código viejo):

              jornada_reprise:jornada_reprise_id(*, reprise:reprise_id(*)),
            .in('jornada_reprise_id', jueceIds);
                  <td>{comp.jornada_reprise?.reprise?.nombre}</td>


